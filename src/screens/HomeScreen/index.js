import React from 'react';
import Feed from '../../components/feed';
import { 
   
    SafeAreaView
  } from 'react-native';


const HomeScreen = () => {
    return(
        
    <SafeAreaView>
        <Feed/>
    </SafeAreaView>
    );
}

export default HomeScreen;